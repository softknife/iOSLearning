//: [Previous](@previous)

import Foundation

/**
 # Type Alias（类型别名）
 - Typealias 是一个能让我们的写代码的时候避免一直重复的实用小工具。有这样一个函数，参数是一个闭包，
 */
typealias ErrorCode = Int

typealias ParameterBlock = (Int,String)->()

// 比较有名气的typealias
typealias Void = ()

typealias NSTimeInterval = Double
